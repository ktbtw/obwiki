<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.obwiki.mapper.CommentMapper">
    <insert id="insert" parameterType="com.example.obwiki.entity.Comment">
        INSERT INTO comment(post_id, user_id, content, create_time)
        VALUES (#{postId}, #{userId}, #{content}, NOW())
    </insert>

    <select id="selectByPostId" parameterType="long" resultType="com.example.obwiki.entity.Comment">
        SELECT * FROM comment WHERE post_id = #{postId} ORDER BY create_time ASC
    </select>
</mapper> 